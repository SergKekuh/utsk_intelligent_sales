name: Build and Push Docker Image

on:
  push:
    branches: [ main ]
    paths:
      - 'utsk_web/**'

jobs:
  docker:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        working-directory: utsk_web
        run: |
          docker build -t utsk-web:latest -f docker/Dockerfile .
          echo "✅ Docker image built"
      
      # Для push'а в Docker Hub — раскомментировать:
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      # 
      # - name: Push to Docker Hub
      #   run: |
      #     docker tag utsk-web:latest your-username/utsk-web:latest
      #     docker push your-username/utsk-web:latest
